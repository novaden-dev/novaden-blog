---
import Pane from "@/layouts/Pane.astro";
---

<Pane title="not found" head="404 Not Found" path={[{ label: "404" }]}>
  <div class="pane-empty">
    <p class="code">404</p>
    <p>No such file. It may have been renamed, or it never existed.</p>
    <a class="back" href="/">← back to the index</a>
  </div>
</Pane>
